/**
 * Cross-promotional external links, shared between AppHeader.vue and
 * AppFooter.vue so both point at the same URL.
 */

/** Sibling tool from the same builder — linked from both the nav and the footer, opens in a new tab. */
export const PDF_TOOL_PH_URL = 'http://pdf-tool-ph.com/'
